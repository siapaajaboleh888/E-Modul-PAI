<div class="mb-4">
  <h2 class="mb-1">Modul Pembelajaran</h2>
  <p class="text-muted mb-0" style="max-width: 720px; text-align: justify;">
    Pilih modul PAI yang ingin Anda pelajari. Bacalah deskripsi singkat setiap modul sebelum masuk ke unit pembelajaran.
  </p>
</div>

<div class="row g-3">
  <% if (modules && modules.length) { %>
    <% modules.forEach(function(m) { %>
      <div class="col-sm-12 col-md-6 col-lg-4">
        <div class="card h-100 shadow-sm border-0">
          <div class="card-body d-flex flex-column">
            <h5 class="card-title mb-2" style="text-align: justify; line-height: 1.3;"><%= m.title %></h5>
            <p class="card-text text-muted mb-3" style="text-align: justify; line-height: 1.4;"><%= m.description || '-' %></p>
            <div class="mt-auto">
              <a href="/student/modules/<%= m.id %>" class="btn btn-primary btn-sm">Lihat Unit</a>
            </div>
          </div>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <div class="col-12 text-muted">Belum ada modul yang tersedia.</div>
  <% } %>
</div>
